# 新番更新时间表小程序 - 项目规格说明书

## 1. 项目概述

### 1.1 项目名称
**新番时间表** - 新番动漫更新时间管理小程序

### 1.2 项目类型
微信小程序（WeChat Mini Program）

### 1.3 核心功能概述
帮助动漫爱好者追踪新番动画的每周更新时间，管理追番列表，记录观看进度。

### 1.4 目标用户
- 动漫爱好者
- 需要追踪多部新番更新时间的用户
- 希望管理个人追番进度的观众

---

## 2. 功能需求

### 2.1 周表视图（核心功能）
**功能描述**：以周为单位展示每天的新番播出时间表

**具体需求**：
- 显示当前周的日期范围（周一至周日）
- 按播出日期分组展示新番
- 每部番剧显示：名称、播出时间、播出平台、当前集数
- 支持切换查看不同周的节目表
- 标记今日更新的番剧
- 支持按时间排序

**用户交互**：
- 点击番剧卡片进入详情页
- 左右滑动切换周
- 下拉刷新获取最新数据

### 2.2 新番总览
**功能描述**：展示当前季度所有新番的完整列表

**具体需求**：
- 网格/列表两种视图切换
- 按季度分类（春季、夏季、秋季、冬季）
- 显示番剧基本信息：名称、类型、播出平台、更新时间
- 支持搜索番剧名称
- 支持按类型筛选（热血、恋爱、搞笑、科幻等）
- 热门推荐标记

**数据字段**：
- 番剧名称（日文、中文、罗马音）
- 封面图片
- 播出开始日期
- 播出平台
- 每周播出日和时间
- 番剧类型标签
- 追番人数/热度指数
- 简介

### 2.3 追番订阅
**功能描述**：用户订阅感兴趣的新番，获取更新提醒

**具体需求**：
- 订阅/取消订阅番剧
- 显示已订阅番剧的列表
- 设置订阅通知提醒
- 显示订阅番剧的下一集更新时间
- 订阅数量统计

**用户交互**：
- 在新番总览页面点击订阅按钮
- 在番剧详情页订阅
- 一键订阅当日更新番剧

### 2.4 追番进度管理
**功能描述**：跟踪用户观看各番剧的进度

**具体需求**：
- 记录当前观看集数
- 标记"想看"、"在看"、"已看完"
- 观看历史记录
- 显示追上进度的番剧（最新集数 vs 已看集数）
- 支持批量管理进度

**状态定义**：
- 想看：添加到待看列表
- 在看：正在追番，记录观看进度
- 已看完：完结后标记
- 搁置：暂时停止观看
- 弃番：放弃追番

---

## 3. 页面结构

### 3.1 页面清单
1. **首页/周表** (`pages/index/index`) - 底部Tab：周表
2. **新番总览** (`pages/overview/overview`) - 底部Tab：新番
3. **订阅管理** (`pages/subscribe/subscribe`) - 底部Tab：订阅
4. **追番进度** (`pages/progress/progress`) - 底部Tab：进度
5. **番剧详情** (`pages/anime-detail/anime-detail`) - 内页

### 3.2 页面跳转关系
```
首页(周表)
├── 番剧详情
│   └── 订阅/取消订阅
│
新番总览
├── 搜索结果
├── 筛选结果
└── 番剧详情
│   └── 订阅/取消订阅
│
订阅管理
├── 番剧详情
│
追番进度
└── 番剧详情
```

---

## 4. 数据结构设计

### 4.1 番剧数据结构
```javascript
{
  id: String,              // 唯一标识
  name: String,             // 中文名称
  nameJp: String,           // 日文名称
  nameEn: String,           // 罗马音
  cover: String,            // 封面图片URL
  platform: String,         // 播出平台
  broadcastDay: Number,     // 播出日（1-7，周一至周日）
  broadcastTime: String,    // 播出时间（HH:mm）
  startDate: String,        // 播出开始日期
  endDate: String,          // 预计结束日期（可选）
  genre: Array<String>,     // 类型标签
  currentEpisode: Number,   // 当前集数
  totalEpisodes: Number,     // 总集数
  status: String,           // 状态：airing（播出中）/ finished（已完结）/ upcoming（即将播出）
  season: String,           // 季度（2024spring/2024summer等）
 热度: Number,              // 热度指数
  简介: String,             // 番剧简介
  追番人数: Number          // 追番人数
}
```

### 4.2 订阅数据结构
```javascript
{
  animeId: String,          // 番剧ID
  subscribedAt: Number,     // 订阅时间戳
  notificationEnabled: Boolean  // 是否开启通知
}
```

### 4.3 进度数据结构
```javascript
{
  animeId: String,          // 番剧ID
  status: String,           // 观看状态
  currentEpisode: Number,    // 当前观看集数
  lastWatchedAt: Number,     // 最后观看时间戳
  rating: Number,            // 用户评分（可选）
  notes: String              // 用户备注（可选）
}
```

---

## 5. UI/UX 设计要求

### 5.1 配色方案
- **主色调**：#4A90D9（蓝色 - 代表清新、活力）
- **辅助色**：#67C23A（绿色 - 已订阅状态）
- **警示色**：#F56C6C（红色 - 取消订阅、删除）
- **背景色**：#F7F8FA（浅灰 - 页面背景）
- **卡片背景**：#FFFFFF（白色）
- **文字主色**：#333333
- **文字辅助**：#999999

### 5.2 字体规范
- 标题：32rpx，加粗
- 正文：28rpx，常规
- 辅助信息：24rpx
- 使用系统默认字体栈

### 5.3 间距规范
- 页面边距：20rpx
- 卡片间距：20rpx
- 卡片内边距：30rpx
- 列表项间距：16rpx

### 5.4 圆角规范
- 卡片圆角：16rpx
- 按钮圆角：40rpx
- 标签圆角：8rpx

---

## 6. 技术实现要求

### 6.1 技术栈
- 微信小程序原生框架
- WXML + WXSS + JavaScript
- 小程序云开发或本地数据存储

### 6.2 数据获取
- 模拟数据（开发阶段）
- 可扩展支持API对接Bangumi、蜜柑计划等数据源

### 6.3 本地存储
- 使用 wx.setStorageSync / wx.getStorageSync
- 存储订阅列表和进度数据

### 6.4 组件化
- 建议封装的组件：
  - AnimeCard（番剧卡片）
  - WeekDaySelector（星期选择器）
  - EpisodeBadge（集数徽章）
  - StatusTag（状态标签）
  - SubscribeButton（订阅按钮）

---

## 7. 里程碑计划

### Phase 1：基础框架搭建
- [x] 项目结构初始化
- [ ] 配置文件创建
- [ ] TabBar导航设置
- [ ] 页面骨架搭建

### Phase 2：周表视图开发
- [ ] 首页布局设计
- [ ] 模拟数据填充
- [ ] 周切换功能
- [ ] 番剧卡片组件

### Phase 3：新番总览开发
- [ ] 列表/网格视图切换
- [ ] 搜索功能
- [ ] 筛选功能
- [ ] 季度分类

### Phase 4：订阅功能开发
- [ ] 订阅/取消订阅逻辑
- [ ] 订阅列表展示
- [ ] 订阅管理页面

### Phase 5：进度管理开发
- [ ] 进度记录功能
- [ ] 状态管理
- [ ] 进度展示

### Phase 6：详情页开发
- [ ] 番剧详情页
- [ ] 订阅/进度入口
- [ ] 分享功能

---

## 8. 备注

- 开发过程中保持代码规范和注释
- 注意小程序包大小限制（主包不超过2M）
- 图片资源建议使用云存储或压缩处理
- 考虑无网络情况下的离线数据展示
